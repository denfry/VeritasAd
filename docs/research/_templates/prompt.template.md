---
id: <slug>
title: <заголовок промпта>
phase: shared
status: planned
date: <YYYY-MM-DD>
author: <автор>
related: []
---

> **Статус: ПЛАНИРУЕТСЯ.** <одна строка контекста: какую задачу решает этот промпт и в каком треке используется>

<!--
Шаблон для артефакта типа PROMPT (системный или исследовательский промпт).
Скопируйте этот файл, переименуйте в `<slug>.md` (с суффиксом версии `-vN` при необходимости),
заполните frontmatter и секции ниже, удалите комментарии и плейсхолдеры в `<...>`.
Для промптов поле `milestone` НЕ указывается. Значение `phase` выберите из: shared | master | phd.
Размещение: общие промпты — `prompts/shared/`; исследовательские промпты — `prompts/master/` или `prompts/phd/`.
-->

## Назначение

<Для чего этот промпт: какую конкретную задачу он выполняет, какую роль играет модель.>

## Контекст применения

<Когда и где используется: на каком этапе пайплайна, в каком сервисе/ноутбуке/эксперименте, при каких условиях вызывается.>

## Вход

<Что подаётся на вход: переменные, шаблонные подстановки, формат входных данных. Опишите каждый плейсхолдер.>

## Выход

<Ожидаемый формат и структура ответа: строгая схема (JSON/таблица/текст), обязательные поля, ограничения. При структурированном выходе приведите схему.>

```json
{
  "<ключ>": "<описание ожидаемого значения>"
}
```

## Текст промпта

<Полный текст промпта. Используйте плейсхолдеры в фигурных скобках для подстановок, напр. {input_text}.>

```text
<system / user prompt здесь>
```

## Few-shot примеры

<Опционально. Один-два показательных примера пар вход → выход, демонстрирующих желаемое поведение. Удалите секцию, если примеры не нужны.>

```text
Вход:
<пример входа>

Выход:
<пример выхода>
```

## Версия и модель

<Версия промпта в формате -vN (напр. v1). Целевая модель / семейство (напр. Claude Opus). Заметки о совместимости и о том, что изменилось между версиями.>

## Связанные документы

<Ссылки на связанные артефакты: спецификации, эксперименты, отчёты, использующие этот промпт.>

- Назад к хабу: [../README.md](../README.md)
