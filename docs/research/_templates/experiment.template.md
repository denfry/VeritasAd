---
id: <stable-kebab-id>
title: <Название эксперимента>
phase: master
milestone: M1
status: planned
date: YYYY-MM-DD
author: <Имя автора>
related: []
---

> **Статус: ЗАПЛАНИРОВАН.** <Одна строка контекста: что проверяет этот эксперимент и зачем.>

> Это шаблон **определения** эксперимента (config). Скопируйте файл под датируемым именем `YYYY-MM-DD-<milestone>-<slug>.md`, заполните плейсхолдеры и удалите эту строку. Результаты сюда **не** пишем — они идут в отдельный отчёт (см. раздел «Связанный отчёт»).

## Гипотеза

<Сформулируйте проверяемую гипотезу одним-двумя предложениями. Что мы ожидаем увидеть и почему? Гипотеза должна быть фальсифицируемой: укажите ожидаемое направление эффекта и порог, при котором результат считается подтверждённым.>

## Setup (метод/модель, окружение, сиды, версии)

- **Метод/модель:** <какой подход, архитектура или модель проверяется; ключевые гиперпараметры.>
- **Окружение:** <ОС, GPU/CPU, объём памяти; менеджер зависимостей и lock-файл (например `uv.lock`).>
- **Сиды:** <фиксированные random seeds для воспроизводимости; число повторов прогона.>
- **Версии:** <версии ключевых библиотек, чекпоинтов моделей, ревизия кода (git SHA).>

## Датасет и split (ссылка на dataset-документацию; train/val/test)

- **Документация датасета:** <относительная ссылка на dataset-документ, например `../datasets/<dataset>.md`.>
- **Split:** <как делится выборка на train / val / test; пропорции, стратегия (random / стратифицированный / temporal), сид сплита.>
- **Размеры:** <число примеров в train / val / test.>

## Baseline (с чем сравниваем)

<Опишите baseline: тривиальный (majority / random), предыдущая версия системы или внешний эталон. Укажите его конфигурацию и где зафиксированы его метрики. Эксперимент имеет смысл только в сравнении с baseline.>

## Метрики

- <Метрика 1 — например precision / recall / F1 для целевого класса.>
- <Метрика 2 — например accuracy / macro-F1.>
- <Метрика 3 — доменная метрика (например точность извлечения ERID, calibration, latency).>
- <Метрика 4 — стоимость/ресурсы (время прогона, токены, $ при использовании LLM).>

## Команда запуска

```bash
# Плейсхолдер команды запуска. Замените на реальный вызов.
python -m veritasad.experiments.<module> \
  --config configs/<this-experiment>.yaml \
  --seed 42 \
  --output artifacts/<milestone>/<this-experiment>/
```

## Артефакты

- **Конфиг:** <куда складывается итоговый конфиг эксперимента, например `configs/<this-experiment>.yaml`.>
- **Результаты/прогоны:** <директория с логами, метриками и чекпоинтами, например `artifacts/<milestone>/<this-experiment>/`.>
- **Воспроизводимость:** <зафиксированный lock-файл и git SHA, при которых получены результаты.>

## Связанный отчёт

<Относительная ссылка на соответствующий report с результатами этого эксперимента, например `../reports/YYYY-MM-DD-<milestone>-<slug>-report.md`. Создаётся после прогона.>

---

[К навигационному хабу research-воркспейса](../README.md)
