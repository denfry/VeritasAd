# Single source of truth: backend/pyproject.toml + backend/uv.lock.
# This file is kept for compatibility with legacy `pip install -r requirements.txt` flows.
# Keep it minimal to avoid version drift.
-e .
