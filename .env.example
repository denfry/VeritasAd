# VeritasAd Environment Variables для Docker Compose
# Скопируйте этот файл в .env и заполните реальными значениями
# Для локальной разработки Supabase НЕ ТРЕБУЕТСЯ - используется mock-аутентификация

# ==================== POSTGRESQL ====================
POSTGRES_PASSWORD=veritasad123

# ==================== TELEGRAM BOT ====================
TELEGRAM_BOT_TOKEN=your_bot_token_here

# ==================== BOT ADMIN (опционально) ====================
# Telegram user IDs через запятую (числовые ID)
BOT_ADMIN_IDS=

# ==================== SENTRY (опционально) ====================
SENTRY_DSN=

# ==================== SUPABASE (ТОЛЬКО для production) ====================
# Для локальной разработки оставьте пустым - будет использоваться mock-аутентификация
# Взять в Supabase: Project Settings → API
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=

# ==================== DEVELOPMENT / TESTING ====================
# Отключить авторизацию для тестирования функционала (development only!)
# Внимание: Это отключает проверку прав доступа - используйте ТОЛЬКО локально!
# DISABLE_AUTH=true
# NEXT_PUBLIC_DISABLE_AUTH=true

# ==================== LOGGING ====================
# Уровень логирования: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
# Формат логов: json (production) или text (development)
LOG_FORMAT=text

# ==================== BRAND DETECTION ====================
# Путь к JSON/YAML файлу со списком брендов для детектирования
BRANDS_FILE=
# Бренды по умолчанию (JSON массив или comma-separated список)
DEFAULT_BRANDS=
# Включить OCR для детектирования брендов из текста на видео
ENABLE_BRAND_OCR=true
# Включить zero-shot детектирование неизвестных брендов/логотипов
ENABLE_ZERO_SHOT=true
# Порог детектирования брендов (0.0-1.0)
BRAND_DETECTION_THRESHOLD=0.12
# Максимум кадров для анализа брендов
BRAND_MAX_FRAMES=50
# Интервал между кадрами для анализа (в секундах)
BRAND_FRAME_INTERVAL=1.0
